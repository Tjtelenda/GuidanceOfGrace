# GitHub handoff

This snapshot is intended to be published to the private repository `Tjtelenda/Telenda-Household` on branch `handoff/guidance-of-grace-v4` under `handoffs/guidance-of-grace-v4/` because the dedicated `Tjtelenda/guidance-of-grace` repository does not yet exist.

This is a temporary handoff location only. Work should create the dedicated private repository, move this project there with history preserved where practical, and update `package.json` release publishing only after the dedicated repo exists.

Do not merge the handoff folder into the household runtime project. Do not commit `.co2`, `.sl2`, `.bak`, extracted FromSoftware assets, npm cache folders, or build outputs.
